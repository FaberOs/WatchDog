package com.unicauca.watchdog.ddd.command.model;

import jakarta.annotation.Priority;

public class ProblemCommand {
    /*  Persistir un nuevo problema
    public void save(Problema problema) {
        entityManager.persist(problema);
    }

     Actualizar la prioridad de un problema
    public void updatePriority(Long problemaId, Priority newPriority) {
        Problema problema = entityManager.find(Problema.class, problemaId);
        problema.setPriority(newPriority);
        entityManager.merge(problema);
    }*/
}
